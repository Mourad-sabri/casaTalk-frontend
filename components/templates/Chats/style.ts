import styled from "@emotion/styled";
import { Card } from "@mui/material";

export const ChatsContainer = styled(Card)`
  width: 95%;
  height: calc(100vh - 50px);
  margin: auto;
  overflow-y: auto;
`;
